# Mono2Micro Pipeline — minimal dependencies (OpenBLAS-free)
#
# ALL heavy computation (clustering, metrics, similarity) is pure Python.
# Only these two packages are needed:
pandas>=1.5       # CSV read/write only — no matrix ops used
numpy>=1.21       # imported by pandas internally; we do NOT call numpy directly

# Optional (uncomment if you want better Louvain):
# python-louvain>=0.16
# networkx>=3.1
