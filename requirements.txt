# Core
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7

# Optional — better Louvain / Leiden
# python-louvain>=0.16
# leidenalg>=0.10
# networkx>=3.1

# Optional — semantic embeddings (Step 3 of audit)
# transformers>=4.38
# torch>=2.1
# sentence-transformers>=2.6

# Java parsing via subprocess (requires Maven / JavaParser in the JVM)
# jpype1>=1.5          # if calling JavaParser from Python directly

# Load testing (Step 2 of audit)
# jmeter is installed separately (not pip)
